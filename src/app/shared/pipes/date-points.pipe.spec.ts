import { DatePointsPipe } from './date-points.pipe';

describe('DatePointsPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePointsPipe();
    expect(pipe).toBeTruthy();
  });
});
