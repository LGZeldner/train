import { SortNotesPipe } from './sort-notes.pipe';

describe('SortNotesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortNotesPipe();
    expect(pipe).toBeTruthy();
  });
});
